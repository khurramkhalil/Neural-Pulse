# Neural-Pulse
A runtime verification framework using Signal Temporal Logic (STL) to detect and intercept "waffling" hallucinations and Semantically Equivalent Attacks (SECA) in LLMs by monitoring entropy and attention signals.
